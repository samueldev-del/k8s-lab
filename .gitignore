# Kubeconfig and credentials — never commit
kubeconfig
*.kubeconfig
.kube/

# Secret manifests contain plaintext values — never commit
manifests/secret.yaml

# macOS files
.DS_Store

# Editor backups
*.swp
*~
